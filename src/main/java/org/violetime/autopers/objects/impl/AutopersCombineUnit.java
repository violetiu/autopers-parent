package org.violetime.autopers.objects.impl;

import java.util.List;

import java.util.logging.Logger;

import org.violetime.autopers.datatype.AutopersSqlType;
import org.violetime.autopers.mapping.AutopersMappingField;
import org.violetime.autopers.objects.AutopersObject;
import org.violetime.autopers.objects.AutopersObjectJoin;
import org.violetime.autopers.reflect.AutopersReflectClass;

public class AutopersCombineUnit {

	private final static Logger logger=Logger.getLogger(AutopersCombineUnit.class.getName());
	
	
	/**
	 * 安装类型获取对象，目前之前只获取第一个。
	 * @param objects
	 * @param type
	 * @return
	 */
	public static AutopersObject  getObject(List<AutopersObject> objects,Class<?> type) {
	
		
		
		
		return null;
	}
	
	
	
}